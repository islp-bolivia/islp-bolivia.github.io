source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# Plugins comunes (agrega solo los que realmente uses)
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
  # gem "jekyll-paginate"
end
